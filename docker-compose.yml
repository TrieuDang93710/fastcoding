version: '3.8'

services:
   app-prod:
      container_name: fastcodingserver-prod
      image: fastcodingserver-prod:latest
      build:
        context: .
        dockerfile: Dockerfile.prod
      env_file:
        - .env
      ports:
        - '9000:9000'
      profiles:
        - prod
volumes:
  db-data:
      